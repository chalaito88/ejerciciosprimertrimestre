package arrays;

public class pasarcomponentesdearrayconmayus {
    public static void main(String[] args) {
        char myarray [] = { 'a' , 'b' , 'c'};

        System.out.print(" " + Character.toUpperCase(myarray[0]) + Character.toUpperCase(myarray[1]) + Character.toUpperCase(myarray[2]));
    }
}